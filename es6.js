import { add } from './math';
//import { resolve } from 'path';

//import('./dyn*').then( ({ default: min }) => {
//    console.log(`min:${min(-4)}`);
//});

import { max }  from './doest-exist';


console.log(max(6));

let text = 'let should converted to var';
const m = new Map();
console.log(resolve('./hello-world.txt'));
console.log(text, map.entries(), add(1, 2))
console.log(import.meta['someprop-1']);